git clone <url> ==> create the respository to our local workspace

git status  : to check whether the git has initiaziled in our current directory

git add <filename>  : addd the fule "filename" to the staginfg area (to perform commit adn push)

git add . : to add all files  in the directory to the staging area

git commit -m "message" : to commit that it has beeen tracked by git

git push -u origin main  :to push the commited file to the global workspace

git init: to initialize git in a folder